name: build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "~1.26"
          cache: true
      - run: go mod tidy
        env:
          GONOSUMDB: "charm.land/*"
          GONOSUMCHECK: "*"
      - run: go build ./...
        env:
          GONOSUMDB: "charm.land/*"
          GONOSUMCHECK: "*"
      - run: go vet ./...
        env:
          GONOSUMDB: "charm.land/*"
          GONOSUMCHECK: "*"
